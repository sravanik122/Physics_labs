{
  "magnet_strength": {
    "type": "number",
    "units": "relative",
    "min": 0.1,
    "max": 1.0,
    "default": 0.6,
    "description": "Relative strength of the magnet. Higher values indicate stronger magnetic fields."
  },

  "distance_from_object": {
    "type": "number",
    "units": "mm",
    "min": 0,
    "max": 50,
    "default": 10,
    "description": "Distance between the magnet and the object being tested."
  },

  "material_type": {
    "type": "enum",
    "units": "category",
    "values": ["iron", "steel", "nickel", "cobalt", "copper", "aluminum", "plastic", "wood"],
    "default": "iron",
    "description": "Type of material placed near the magnet."
  },

  "pole_orientation": {
    "type": "enum",
    "units": "configuration",
    "values": ["N-S", "N-N", "S-S"],
    "default": "N-S",
    "description": "Orientation of magnetic poles relative to each other."
  },

  "magnetic_field_intensity": {
    "type": "number",
    "units": "tesla",
    "min": 0.01,
    "max": 2.0,
    "default": 0.5,
    "description": "Initial magnetic field intensity of the magnet."
  },

  "temperature": {
    "type": "number",
    "units": "°C",
    "min": 0,
    "max": 100,
    "default": 25,
    "description": "Ambient temperature affecting magnetic strength."
  },

  "shape_of_magnet": {
    "type": "enum",
    "units": "shape",
    "values": ["bar", "horseshoe", "ring", "cylindrical"],
    "default": "bar",
    "description": "Physical shape of the magnet, influencing field distribution."
  },

  "surface_contact": {
    "type": "enum",
    "units": "contact_state",
    "values": ["touching", "not_touching"],
    "default": "not_touching",
    "description": "Whether the magnet is in direct contact with the object."
  },

  "number_of_magnets": {
    "type": "number",
    "units": "count",
    "min": 1,
    "max": 5,
    "default": 1,
    "description": "Number of magnets used together in the experiment."
  },

  "surrounding_medium": {
    "type": "enum",
    "units": "medium",
    "values": ["vacuum", "air", "water"],
    "default": "air",
    "description": "Medium surrounding the magnet and object, affecting field propagation."
  }
}
